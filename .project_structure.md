# Health项目结构说明

## 📁 当前结构

```
Health/
├── core/                    # 核心模块
│   ├── health_agents.py    # Agent定义（旧）
│   ├── health_coordinator.py  # 协调器
│   └── health_orchestrator.py # 编排器（旧）
│
├── client/                 # 客户端接口
│   └── health_client.py
│
├── adapters/               # 数据适配器
│   └── health_data_adapter.py
│
├── Agent/                  # ⚠️ 子系统（建议移到dependencies/）
│   ├── Agent_Perception_Memory_System/
│   ├── Multi-Agent-LLM-System/
│   └── psychology_agent/
│
├── Sports_Injury_Risk/     # ⚠️ 子系统（建议移到dependencies/）
│
├── examples/               # 示例代码
├── main.py                 # 主入口
└── requirements.txt        # 依赖
```

## 🎯 优化建议

### 1. 统一Agent模块
将 `core/health_agents.py` 拆分为：
```
core/
├── agents/
│   ├── __init__.py
│   ├── llm_text.py      # LLM文本Agent
│   └── multimodal.py    # 多模态Agent
└── coordinator.py       # 协调器
```

### 2. 子系统独立
创建 `dependencies/` 目录，移动子系统：
```
dependencies/
├── perception_memory/
├── multi_agent_llm/
├── psychology/
└── sports_injury/
```

### 3. 添加标准目录
```
├── tests/          # 测试
├── docs/           # 文档
└── configs/        # 配置
```

## 📝 快速优化脚本

```bash
# 1. 创建新目录
mkdir -p core/agents dependencies tests docs configs

# 2. 移动子系统（可选）
# mv Agent/* dependencies/
# mv Sports_Injury_Risk dependencies/sports_injury

# 3. 拆分Agent文件
# 手动拆分 health_agents.py 到 agents/ 目录
```

